{
  "name": "pact-js-bug-state-params-dropped",
  "version": "1.0.0",
  "description": "Minimal example of a bug in pact-js: the `v3` provider drops state params in `v2` pact file",
  "scripts": {
    "test": "rimraf pacts && mocha"
  },
  "license": "MIT",
  "devDependencies": {
    "@pact-foundation/pact": "^11.0.2",
    "axios": "^0.27.2",
    "chai": "^4.3.6",
    "mocha": "^9.2.2",
    "mocha-pact": "^0.10.1",
    "rimraf": "^3.0.2"
  }
}
